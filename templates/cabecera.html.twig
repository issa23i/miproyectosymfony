{% block header %}
    <h3>Bienvenid@ {{ app.user.username }}</h3>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('familias') }}">Listado de familias</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('cesta') }}">Cesta</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('pedidos') }}">Ver Pedidos</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
{% endblock %}

